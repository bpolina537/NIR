services:
  web:
    build: .
    environment:
      EXERCISE_LOGIN: "${EXERCISE_LOGIN:-}"
      EXERCISE_PASSWORD: "${EXERCISE_PASSWORD:-}"
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - ./public:/var/www/html
